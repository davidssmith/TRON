The whole body data set used in the poster is too large for Github, so I’ve included temporarily an abdominal only set. Eventually, I will add a link to the whole body data set so the results of poster.

